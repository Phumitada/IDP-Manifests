apiVersion: paas.internal/v1
kind: Application
metadata:
  name: claude-domain-test
spec:
  image: null
  port: null
  imagePullSecret: ghcr-secret
  databaseRef: []
  domain: claude-test.example.com
  envSecretRefs:
    - claude-domain-test-env-secret
